# 인수인계 — hr-news-agent

**이 문서는 "여기까지 어떻게 왔는지"와 "남은 일"입니다.**
코드 구조·디자인 규칙·검증 절차는 `CLAUDE.md`에 있습니다. 겹치지 않게 나눠 두었으니
둘 다 읽고, 고칠 때도 해당하는 쪽만 고치세요.

---

## 1. 무엇을 만들고 있나

인사팀용 데일리 뉴스 브리핑 에이전트. 평일 아침 10시(KST)에 인사·노무 뉴스를 모아
요약하고, 팀원에게 메일을 보내고, 결과를 공개 아카이브 사이트에 쌓습니다.

사용자는 인사관리실 총괄. 개발 전담 인력 없이 AI 보조로 직접 코드를 다룹니다.
회사는 마케팅 SaaS 를 만드는 IT 기업이고, 팀원 전체가 읽는 사내 공유용입니다.
사이트 자체는 공개 뉴스 요약이라 비상업·공개로 운영합니다.

```
GitHub Actions (평일 10:00 KST, 월요일은 주말 포함 3일치)
  ├─ 구글뉴스 RSS 25개 키워드 수집 → 중복 제거
  ├─ LLM 요약·분류 (Gemini 무료 티어)
  ├─ Gmail SMTP 로 팀원에게 BCC 발송
  ├─ content/YYYY-MM-DD.json 커밋   ← 이게 곧 아카이브
  └─ Next.js 정적 빌드 → GitHub Pages 배포
```

전부 무료 범위. 도메인만 안 사면 0원입니다.

---

## 2. 이미 정해진 결정 (뒤집지 말 것)

**DB를 쓰지 않는다.** `content/YYYY-MM-DD.json` 커밋이 곧 아카이브이고 git 히스토리가
곧 버전 관리입니다. Supabase나 다른 DB를 다시 제안하지 마세요.

**Vercel이 아니라 GitHub 하나로 끝낸다.** Vercel Hobby는 비상업 용도 약관 이슈가 있고
함수 실행시간 제한이 빡빡합니다. Actions는 6시간까지 되고 퍼블릭 저장소는 무제한입니다.

**기사 전문을 저장하거나 표시하지 않는다.** 제목 + 자체 요약 3~4문장 + 원문 링크까지만.
저작권 문제라 타협 대상이 아닙니다.

**링크와 제목은 LLM이 만들지 않는다.** `summarize.mjs`는 LLM에게 입력 배열의
인덱스(`id`)만 돌려받고, 제목·URL·매체는 원본 데이터에서 매핑합니다. 매일 무인으로
도는 시스템이라 링크 환각을 구조적으로 차단한 것입니다. **이 구조를 깨지 마세요.**

**저장과 배포는 메일 실패에 막히지 않는다.** `daily.yml`의 커밋·빌드·배포 단계에
`if: ${{ !cancelled() }}`가 붙어 있습니다. 2026-08-30 에 메일 실패로 그날 커밋과 배포가
통째로 건너뛰어진 적이 있어서 넣은 조건입니다. 지우지 마세요.

**산업 동향은 하루 최대 3건.** 인사 기사 자리를 잠식하지 않게 프롬프트에 상한이
박혀 있습니다. 이 브리핑의 중심은 어디까지나 인사·노무입니다.

**접근 제한은 걸지 않는다.** 공개 뉴스 요약이고 코멘트 기능도 없어서 그냥 공개합니다.

---

## 3. 현재 상태 — 운영 중

**2026-08-30 첫 발행 완료.** 설정과 첫 실행이 모두 끝났고 자동으로 돌고 있습니다.

| 항목 | 값 |
|---|---|
| 저장소 | github.com/monolisa2/hr-news-agent (Public) |
| 사이트 | https://monolisa2.github.io/hr-news-agent/ |
| 발송 | 평일 10:00 KST, Gmail BCC 7명 |
| 모델 | `gemini-3.6-flash` |
| 첫 호 | 2026-08-30 제1호 |

등록된 Secrets: `GEMINI_API_KEY`, `SMTP_USER`, `SMTP_PASS`, `MAIL_TO`
등록된 Variables: `SITE_URL`

수집은 **구글뉴스 RSS 25개 키워드만** 씁니다. 정부 보도자료 피드 두 곳은 모두 죽어서
`sources.mjs`의 `FEEDS`를 비워 두었습니다(아래 5번 참고).

---

## 4. 남은 일 / 지켜볼 것

며칠 돌려보고 판단할 것들입니다. 급한 건 없습니다.

**빈손인 키워드 정리.** `개발자 연봉`, `스타트업 인사`가 첫 테스트에서 0건이었습니다.
계속 0이면 `IT 개발자 처우`, `테크 스타트업 채용` 같은 것으로 교체하세요.

**산업 동향 비중.** 하루 3건이 많은지 적은지는 며칠 읽어봐야 압니다.
`summarize.mjs` 프롬프트의 상한만 고치면 됩니다.

**요약 문장 품질.** LLM이 만드는 문장이라 가끔 어색한 표현이 섞입니다
(예: "주안점" 대신 "추첨"). 눈에 띄게 잦아지면 프롬프트를 조이거나 모델을 바꾸세요.

**`(추정)` 꼬리표.** `impact` 문장 끝에 자주 붙습니다. 프롬프트의
"추측이면 추측이라고 밝히세요" 지시 때문인데, 번잡하면 그 문장을 덜어내세요.

**액션 버전 올리기.** `checkout@v4` 등이 Node 20 을 바라봐서 deprecation 경고가 뜹니다.
GitHub 이 알아서 Node 24 로 돌려주고 있어 당장 할 일은 없습니다. 새 메이저 버전이
나오면 그때 올리세요.

**Next 의존성.** `postcss` 취약점 2건이 Next 안에 묶여 있습니다. 빌드 시점에만 쓰이고
CSS 는 직접 작성한 것뿐이라 실질 위험이 없습니다. 해결하려면 Next 16 메이저
업그레이드가 필요하니 서두르지 마세요.

---

## 5. 알려진 주의점

**GitHub 크론은 5~20분 늦게 뜹니다.** 10시 정각이 중요하면 `daily.yml`의 cron 을
`0 0 * * 1-5`(KST 09:00)로 당기세요. 월요일은 `run.mjs`가 자동으로 3일치를 수집하므로
주말 뉴스가 빠지지 않습니다.

**정부 보도자료 피드는 2026-08-30 기준 둘 다 죽었습니다.** 고용노동부
`enewsList.do?rssYn=Y` 는 RSS 대신 HTML 을 반환하고, `korea.kr/rss/dept_moel.xml` 은
404 입니다. 대체 주소를 여섯 개 찍어봤지만 전부 실패했습니다. 새 주소를 찾으면
`sources.mjs`의 `FEEDS`에 `{ name, url }` 형태로 다시 넣으세요.

**Gemini 모델명이 바뀝니다.** 기본값은 `gemini-3.6-flash`. `gemini-2.5-flash`는 신규
API 키에서 이미 폐쇄되어 404 가 납니다. 고약한 건 **모델 목록 API 에는 여전히 보인다**는
점이라, 목록만 보고 판단하면 안 됩니다. 요약 단계에서 404 가 나면 Variables 에
`GEMINI_MODEL` 을 추가해 교체하세요.

**60일간 커밋이 없으면 GitHub 이 크론을 멈춥니다.** 이 워크플로는 매일 커밋을 만들어
자동 유지되지만, 실패가 오래 이어졌다면 수동 실행으로 되살려야 합니다.

**메일이 안 오는 두 가지 경우가 다릅니다.**

- **앱 비밀번호가 틀리면** Gmail 인증 거부 → 워크플로가 빨간 X. 눈에 띕니다.
  이제는 커밋·배포가 따로 진행되므로 그날 기록은 남습니다.
- **Secret 이 비어 있거나 이름에 오타가 있으면** `email.mjs`가
  "메일 설정이 없어 발송을 건너뜁니다" 한 줄만 남기고 넘어갑니다.
  **워크플로는 초록불이고 사이트도 정상 갱신되는데 메일만 안 옵니다.**
  메일이 안 왔는데 초록불이면 Secret 이름부터 확인하세요.

**메일에는 웹폰트가 적용되지 않습니다.** 메일 클라이언트가 외부 폰트를 차단합니다.
`email.mjs`의 폰트 목록은 수신자 PC 에 설치된 폰트로만 떨어집니다. 여기 시간 쓰지 마세요.

---

## 6. 이 문서에 없는 것

아래는 전부 `CLAUDE.md` 에 있습니다. 여기에 옮겨 적지 마세요 — 두 곳에 같은 내용이
있으면 한쪽만 고쳤을 때 어느 쪽이 맞는지 알 수 없게 됩니다.

- 파일별 역할과 구조
- 디자인 시스템 (색·폰트·날짜 레일)
- 변경 후 검증 절차
- 자주 나올 요청과 그때 건드릴 파일
- 응답 톤

---

## 이어서 작업할 때

로컬 검증에는 `.env` 가 필요합니다. `.env.example` 을 복사해 `GEMINI_API_KEY` 만
채우면 `--dry-run` 이 돕니다 — 메일도 안 가고 파일도 안 씁니다. 메일까지 확인하려면
`SMTP_USER` / `SMTP_PASS` 도 넣으세요.

`.env` 는 `.gitignore` 에 있어 커밋되지 않습니다.

실행할 명령은 `CLAUDE.md` 의 검증 절차를 따르세요.
